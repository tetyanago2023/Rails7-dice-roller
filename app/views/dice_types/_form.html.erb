<%= form_with(model: dice_type) do |form| %>
  <% if dice_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dice_type.errors.count, "error") %> prohibited this dice_type from being saved:</h2>

      <ul>
        <% dice_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :sides, style: "display: block" %>
    <%= form.number_field :sides %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
